# AgentFlow 模板中心

> 预设的智能工作流模板，一键将创意转化为完整方案

---

## 模板分类

### 💼 商业策划

| 模板 | 描述 | 文件 | 状态 |
|------|------|------|------|
| **商业计划生成器** | 完整商业计划书，包含市场分析、商业模式、执行策略 | [business-plan-generator.json](./business-plan-generator.json) | ✅ 已完成 |
| 副业方案规划 | 零成本副业起步方案 | `side-project-planner.json` | 📝 待开发 |
| 创业BP生成 | 融资用商业计划书 | `startup-bp-generator.json` | 📝 待开发 |
| 竞品分析报告 | 深度竞品研究报告 | `competitor-analysis.json` | 📝 待开发 |

### 📱 内容营销

| 模板 | 描述 | 文件 | 状态 |
|------|------|------|------|
| **自媒体内容策划** | 账号定位、选题库、发布排期 | [content-strategy-generator.json](./content-strategy-generator.json) | ✅ 已完成 |
| **爆款选题生成** | 批量生成50+选题创意 | [viral-topics-generator.json](./viral-topics-generator.json) | ✅ 已完成 |
| 小红书运营方案 | 小红书专项运营策略 | `xiaohongshu-strategy.json` | 📝 待开发 |
| 短视频脚本批量 | 批量生成视频脚本 | `video-script-generator.json` | 📝 待开发 |

### 🎯 产品规划

| 模板 | 描述 | 文件 | 状态 |
|------|------|------|------|
| **PRD文档生成** | 完整产品需求文档 | [prd-generator.json](./prd-generator.json) | ✅ 已完成 |
| MVP规划方案 | 最小可行产品规划 | `mvp-planner.json` | 📝 待开发 |
| 功能优先级矩阵 | 需求排序和规划 | `feature-prioritization.json` | 📝 待开发 |
| 用户调研方案 | 用户研究方案设计 | `user-research-plan.json` | 📝 待开发 |

### 📊 企业服务

| 模板 | 描述 | 文件 |
|------|------|------|
| 营销方案生成 | 整合营销策略方案 | `marketing-plan.json` |
| 品牌定位策划 | 品牌建设方案 | `brand-positioning.json` |
| 活动策划方案 | 线上线下活动策划 | `event-planning.json` |
| 培训课程设计 | 企业内训课程设计 | `training-course-design.json` |

---

## 模板结构说明

每个模板文件包含以下主要部分：

```json
{
  "id": "模板唯一标识",
  "name": "模板名称",
  "description": "模板描述",
  "category": "分类",
  
  "inputs": {
    "required": [...],  // 必填输入项
    "optional": [...]   // 选填输入项
  },
  
  "outputSections": [...],  // 输出章节定义
  
  "workflow": {
    "nodes": [...],    // 工作流节点
    "edges": [...]     // 节点连接
  },
  
  "example": {
    "input": {...},    // 示例输入
    "outputPreview": "..." // 示例输出预览
  }
}
```

---

## 如何创建自定义模板

### 1. 复制现有模板

```bash
cp business-plan-generator.json my-custom-template.json
```

### 2. 修改基本信息

```json
{
  "id": "my-custom-template",
  "name": "我的自定义模板",
  "description": "模板描述",
  "category": "business"
}
```

### 3. 定义输入字段

```json
"inputs": {
  "required": [
    {
      "id": "main_input",
      "label": "主要输入",
      "type": "textarea",
      "placeholder": "提示文字",
      "validation": {
        "required": true,
        "minLength": 10
      }
    }
  ]
}
```

### 4. 设计工作流

在 `workflow` 部分定义节点和连接关系。

### 5. 测试和发布

在本地测试模板后，可以提交到模板市场分享。

---

## 输入字段类型

| 类型 | 说明 | 适用场景 |
|------|------|----------|
| `text` | 单行文本 | 短文本输入 |
| `textarea` | 多行文本 | 长文本、描述 |
| `number` | 数字 | 数值输入 |
| `select` | 单选下拉 | 固定选项选择 |
| `multiselect` | 多选 | 多个选项选择 |
| `slider` | 滑块 | 范围值选择 |
| `switch` | 开关 | 是/否选择 |

---

## 节点类型

| 类型 | 说明 | 用途 |
|------|------|------|
| `start` | 开始节点 | 工作流入口 |
| `end` | 结束节点 | 工作流出口 |
| `llm` | LLM调用 | AI生成内容 |
| `web_search` | 联网搜索 | 获取实时数据 |
| `template` | 模板渲染 | 整合内容 |
| `condition` | 条件分支 | 流程控制 |
| `loop` | 循环 | 重复执行 |

---

## 变量引用语法

在节点配置中，使用 `{{node_id.output_field}}` 引用其他节点的输出：

```
{{start.idea}}           - 引用开始节点的 idea 字段
{{input_parser.domain}}  - 引用 input_parser 节点的 domain 输出
{{web_search.results}}   - 引用搜索结果
```

---

## 最佳实践

1. **输入越详细，输出越精准** - 设计足够多的输入字段
2. **分章节生成** - 避免一次生成过长内容
3. **使用联网搜索** - 获取最新数据增强可信度
4. **提供示例** - 帮助用户理解如何使用
5. **迭代优化** - 根据用户反馈持续改进提示词

---

*模板持续更新中，欢迎贡献新模板！*

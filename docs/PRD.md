# AgentFlow 产品需求文档 (PRD)

> **文档版本**: v1.0  
> **创建日期**: 2026-01-26  
> **产品名称**: AgentFlow（暂定）  
> **产品定位**: 本地优先、代码级自定义、社区驱动的AI Agent工作流平台

---

## 目录

1. [产品概述](#1-产品概述)
2. [市场分析](#2-市场分析)
3. [目标用户](#3-目标用户)
4. [核心功能需求](#4-核心功能需求)
5. [系统架构设计](#5-系统架构设计)
6. [功能模块详细说明](#6-功能模块详细说明)
7. [技术规格](#7-技术规格)
8. [数据模型设计](#8-数据模型设计)
9. [API设计规范](#9-api设计规范)
10. [安全与隐私](#10-安全与隐私)
11. [商业模式](#11-商业模式)
12. [开发路线图](#12-开发路线图)
13. [成功指标](#13-成功指标)
14. [风险评估](#14-风险评估)
15. [附录](#15-附录)

---

## 1. 产品概述

### 1.1 产品愿景

打造一个**人人可用、高度自定义、隐私优先**的AI Agent工作流平台，让用户能够：
- 通过可视化方式快速构建AI自动化工作流
- 拥有代码级别的自定义能力满足专业需求
- 在本地安全运行敏感工作流，数据完全自主可控
- 参与社区生态，分享和发现优质Agent

### 1.2 产品定位

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                    AgentFlow 产品定位矩阵                        │
│                                                                 │
│     专业性高 ↑                                                   │
│              │           ┌─────────────┐                        │
│              │           │  AgentFlow  │  ← 我们的位置           │
│              │           │  (目标)     │                        │
│              │           └─────────────┘                        │
│              │    ┌─────┐                                       │
│              │    │Dify │                                       │
│              │    └─────┘      ┌─────┐                          │
│              │                 │Coze │                          │
│              │                 └─────┘                          │
│              │  ┌──────┐              ┌───────┐                 │
│              │  │n8n   │              │ Manus │                 │
│              │  └──────┘              └───────┘                 │
│     专业性低 ├──────────────────────────────────→ 易用性高       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 核心价值主张

| 价值点 | 描述 | 竞争优势 |
|--------|------|----------|
| **本地优先** | 支持完全本地运行，数据不出本机 | Coze/Manus均为纯云端 |
| **代码级自定义** | 用户可编写自定义节点和插件 | 超越拖拽式工具的局限 |
| **社区生态** | Agent商店+分享机制+创作者经济 | 形成增长飞轮 |
| **极致体验** | 5分钟上手，美观现代的界面 | 降低使用门槛 |

---

## 2. 市场分析

### 2.1 市场规模

- **全球AI Agent市场规模**: 2025年约$50亿，预计2030年达$280亿（CAGR 40%+）
- **目标市场**: 企业自动化、个人效率工具、开发者工具
- **中国市场特点**: 对数据安全和本地部署需求更强烈

### 2.2 竞品分析

#### 2.2.1 主要竞品对比

| 维度 | Manus | Coze | Dify | n8n | AgentFlow(目标) |
|------|-------|------|------|-----|-----------------|
| **核心定位** | AI Agent执行 | Bot构建平台 | LLM应用开发 | 工作流自动化 | AI工作流+生态 |
| **部署方式** | 云端 | 云端 | 云端/私有 | 云端/私有 | 本地/云端/混合 |
| **自定义能力** | 低 | 中 | 中高 | 高 | 极高 |
| **社区生态** | 无 | Bot商店 | 模板市场 | 社区模板 | Agent商店+创作者经济 |
| **上手难度** | 简单 | 简单 | 中等 | 较高 | 简单 |
| **开源** | 否 | 否 | 是 | 是 | 计划开源核心 |
| **本地LLM** | 否 | 否 | 有限 | 是 | 原生支持 |

#### 2.2.2 竞品优劣势分析

**Manus**
- ✅ 优势：执行能力强，用户体验流畅
- ❌ 劣势：黑盒模式，无法自定义，数据安全顾虑

**Coze（扣子）**
- ✅ 优势：字节生态，Bot发布便捷，中文支持好
- ❌ 劣势：节点类型固定，企业级功能有限

**Dify**
- ✅ 优势：开源，可私有化，功能完整
- ❌ 劣势：UI偏开发者向，社区功能弱

**n8n**
- ✅ 优势：节点丰富，高度灵活
- ❌ 劣势：AI能力不是原生，学习曲线陡

### 2.3 市场机会

1. **本地部署需求**: 企业和个人对数据安全的关注持续增长
2. **创作者经济**: 缺乏真正的Agent创作者变现平台
3. **专业用户痛点**: 现有工具的自定义能力不足
4. **开发者生态**: 缺乏一个类似npm/pip的Agent生态系统

---

## 3. 目标用户

### 3.1 用户画像

#### 3.1.1 主要用户群体

```
┌─────────────────────────────────────────────────────────────────┐
│                      用户金字塔                                  │
│                                                                 │
│                         /\                                      │
│                        /  \         插件开发者                   │
│                       / 5% \        - 发布自定义节点              │
│                      /──────\       - 参与生态建设               │
│                     /        \                                   │
│                    /   15%    \     高级用户                     │
│                   /────────────\    - 复杂workflow              │
│                  /              \   - 代码节点使用               │
│                 /      30%      \   普通用户                     │
│                /──────────────────\  - 使用模板                  │
│               /                    \ - 简单自定义                │
│              /        50%          \ 浏览者                      │
│             /────────────────────────\ - 使用现成Agent           │
│                                        - 偶尔分享                │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.1.2 用户角色定义

**角色1: 效率追求者（小白用户）**
- **特征**: 非技术背景，追求效率提升
- **需求**: 开箱即用的Agent，简单配置即可使用
- **场景**: 每日新闻摘要、邮件自动分类、会议纪要生成
- **痛点**: 不懂代码，害怕复杂配置

**角色2: 业务自动化者（普通用户）**
- **特征**: 有一定技术基础，负责业务流程
- **需求**: 可视化搭建工作流，连接多个系统
- **场景**: CRM数据同步、报表自动生成、客户跟进提醒
- **痛点**: 现有工具不够灵活，集成能力有限

**角色3: 技术探索者（高级用户）**
- **特征**: 开发者或技术爱好者
- **需求**: 代码级控制，自定义逻辑
- **场景**: 复杂的多Agent协作、自定义数据处理流程
- **痛点**: 低代码工具限制太多

**角色4: 创作者/变现者（插件开发者）**
- **特征**: 有开发能力，希望分享或变现
- **需求**: 发布和销售自己的Agent/插件
- **场景**: 开发垂直领域Agent、提供定制服务
- **痛点**: 缺乏分发渠道和变现途径

**角色5: 企业管理者（B端决策者）**
- **特征**: 关注ROI和数据安全
- **需求**: 私有化部署、权限管理、审计日志
- **场景**: 团队协作、企业级自动化
- **痛点**: 数据安全合规、成本控制

### 3.2 用户场景

#### 场景矩阵

| 场景类别 | 具体场景 | 目标用户 | 优先级 |
|----------|----------|----------|--------|
| **内容创作** | 自动化文章写作、社媒内容生成 | 效率追求者 | P0 |
| **数据处理** | 报表生成、数据清洗、分析摘要 | 业务自动化者 | P0 |
| **客户服务** | 智能客服、FAQ自动回复 | 业务自动化者 | P1 |
| **个人助理** | 日程管理、邮件处理、提醒 | 效率追求者 | P0 |
| **开发辅助** | 代码审查、文档生成、Bug分析 | 技术探索者 | P1 |
| **研究分析** | 竞品分析、市场调研、论文阅读 | 业务自动化者 | P1 |
| **企业集成** | ERP/CRM集成、审批流程自动化 | 企业管理者 | P2 |

---

## 4. 核心功能需求

### 4.1 功能架构总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                         AgentFlow 功能架构                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                       前端应用层                              │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │   │
│  │  │ Workflow  │ │  Agent    │ │   社区    │ │   个人    │   │   │
│  │  │  编辑器   │ │   商店    │ │   中心    │ │   中心    │   │   │
│  │  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              ↓                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                       核心功能层                              │   │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────┐ │   │
│  │  │    节点系统      │  │    执行引擎     │  │   插件系统   │ │   │
│  │  │  - 内置节点     │  │  - 任务调度     │  │  - 加载器   │ │   │
│  │  │  - 自定义节点   │  │  - 状态管理     │  │  - 沙箱     │ │   │
│  │  │  - 节点市场     │  │  - 错误处理     │  │  - 市场     │ │   │
│  │  └─────────────────┘  └─────────────────┘  └─────────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              ↓                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                       基础设施层                              │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │   │
│  │  │  双模式运行时 │  │   数据存储   │  │      AI服务        │ │   │
│  │  │  - 本地模式  │  │  - 云端DB   │  │  - 多模型支持      │ │   │
│  │  │  - 云端模式  │  │  - 本地DB   │  │  - 本地模型集成    │ │   │
│  │  │  - 混合模式  │  │  - 同步层   │  │  - 模型路由       │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.2 功能优先级矩阵

```
重要性高 ↑
         │  ┌─────────────────┐   ┌─────────────────┐
         │  │  P0 - 必须有    │   │  P1 - 应该有    │
         │  │                 │   │                 │
         │  │ • 可视化编辑器   │   │ • 自定义节点SDK │
         │  │ • 基础节点集    │   │ • Agent商店     │
         │  │ • 用户系统      │   │ • 分享机制      │
         │  │ • 执行引擎      │   │ • 本地模型支持  │
         │  │ • 云端执行      │   │ • 调试工具      │
         │  │                 │   │                 │
         │  └─────────────────┘   └─────────────────┘
         │  ┌─────────────────┐   ┌─────────────────┐
         │  │  P2 - 可以有    │   │  P3 - 锦上添花  │
         │  │                 │   │                 │
         │  │ • 团队协作      │   │ • 移动端       │
         │  │ • 插件市场      │   │ • CLI工具      │
         │  │ • 企业版功能    │   │ • 高级分析      │
         │  │ • 实时协作编辑  │   │ • AI助手搭建   │
         │  │                 │   │                 │
         │  └─────────────────┘   └─────────────────┘
         └────────────────────────────────────────────→ 紧急程度高
```

### 4.3 P0功能详细需求

#### 4.3.1 可视化Workflow编辑器

**功能描述**: 拖拽式的工作流画布，用户可以通过可视化方式构建AI工作流

**功能需求**:

| 需求ID | 功能点 | 描述 | 验收标准 |
|--------|--------|------|----------|
| ED-001 | 画布基础 | 无限画布，支持缩放、平移 | 缩放范围25%-400%，平滑过渡 |
| ED-002 | 节点拖拽 | 从节点面板拖拽节点到画布 | 拖拽流畅，有吸附效果 |
| ED-003 | 连线操作 | 节点间连线，支持曲线/直线 | 连线有动画，类型匹配检查 |
| ED-004 | 节点配置 | 点击节点弹出配置面板 | 配置实时生效，有校验提示 |
| ED-005 | 撤销重做 | Ctrl+Z/Ctrl+Y | 支持50步历史记录 |
| ED-006 | 复制粘贴 | 节点和节点组的复制粘贴 | 支持跨workflow粘贴 |
| ED-007 | 快捷键 | 常用操作快捷键 | 提供快捷键帮助面板 |
| ED-008 | 小地图 | 画布缩略图导航 | 右下角显示，可折叠 |
| ED-009 | 自动布局 | 一键美化布局 | 支持多种布局算法 |
| ED-010 | 分组功能 | 将多个节点分组 | 分组可命名，可折叠 |

**交互设计要点**:
- 双击画布空白处快速添加节点
- 拖拽连线时显示兼容的目标端口
- 节点hover显示简要说明
- 连线上可添加标签说明

#### 4.3.2 基础节点集

**节点分类及清单**:

```
基础节点集
├── 🤖 AI节点
│   ├── LLM调用 (Chat)
│   ├── LLM调用 (Completion)
│   ├── 文本嵌入 (Embedding)
│   ├── 图像生成
│   └── 语音转文字
│
├── 🔌 集成节点
│   ├── HTTP请求
│   ├── Webhook触发
│   ├── 数据库查询
│   ├── 发送邮件
│   └── 发送消息 (微信/飞书/钉钉)
│
├── 🔀 逻辑节点
│   ├── 条件判断 (If/Else)
│   ├── 循环 (Loop)
│   ├── 并行执行 (Parallel)
│   ├── 等待 (Delay)
│   └── 错误处理 (Try/Catch)
│
├── 📊 数据节点
│   ├── 变量设置
│   ├── 数据转换 (JSON/XML/CSV)
│   ├── 数据合并
│   ├── 数据筛选
│   └── 数据聚合
│
├── 📝 文本节点
│   ├── 文本模板
│   ├── 正则提取
│   ├── 文本分割
│   └── 文本合并
│
├── 💻 代码节点
│   ├── JavaScript代码
│   ├── Python代码
│   └── 表达式计算
│
└── 📥 输入输出节点
    ├── 开始节点
    ├── 结束节点
    ├── 用户输入
    └── 输出展示
```

**LLM调用节点详细设计**:

| 配置项 | 类型 | 说明 |
|--------|------|------|
| 模型选择 | Select | 支持GPT-4/Claude/本地模型等 |
| System Prompt | Textarea | 系统提示词 |
| User Prompt | Textarea | 用户提示词，支持变量插值 |
| Temperature | Slider | 0-2，默认0.7 |
| Max Tokens | Number | 最大输出token数 |
| 流式输出 | Toggle | 是否启用流式 |
| 超时设置 | Number | 请求超时时间(秒) |
| 重试次数 | Number | 失败重试次数 |

#### 4.3.3 用户系统

**功能需求**:

| 需求ID | 功能点 | 描述 |
|--------|--------|------|
| US-001 | 注册登录 | 邮箱注册、第三方登录(GitHub/Google/微信) |
| US-002 | 个人资料 | 头像、昵称、简介、社交链接 |
| US-003 | Workflow管理 | 我的workflow列表、文件夹管理 |
| US-004 | 收藏功能 | 收藏他人Agent/Workflow |
| US-005 | 使用统计 | 执行次数、API调用量、费用统计 |
| US-006 | API密钥管理 | 管理各AI服务的API密钥 |
| US-007 | 偏好设置 | 主题、语言、默认模型等 |

#### 4.3.4 执行引擎

**核心能力**:

```
执行引擎架构
┌─────────────────────────────────────────────────────────────────┐
│                        执行引擎                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐        │
│  │  任务队列    │ →  │  执行器     │ →  │  结果收集   │        │
│  │  (BullMQ)   │    │  (Worker)   │    │  (Store)    │        │
│  └─────────────┘    └─────────────┘    └─────────────┘        │
│         ↑                  ↑                  ↓                 │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐        │
│  │  调度器     │    │  沙箱环境   │    │  状态管理   │        │
│  │  (触发)     │    │  (隔离)     │    │  (持久化)   │        │
│  └─────────────┘    └─────────────┘    └─────────────┘        │
│                                                                 │
│  执行模式:                                                      │
│  ├── 即时执行: 用户触发立即执行                                  │
│  ├── 定时执行: Cron表达式调度                                    │
│  ├── 事件触发: Webhook/消息触发                                  │
│  └── 手动触发: 测试/调试时使用                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**执行状态**:
- `pending` - 等待执行
- `running` - 执行中
- `paused` - 已暂停
- `completed` - 执行完成
- `failed` - 执行失败
- `cancelled` - 已取消

**日志与监控**:
- 每个节点的输入输出记录
- 执行耗时统计
- 错误堆栈追踪
- 实时执行进度展示

---

## 5. 系统架构设计

### 5.1 整体架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              客户端层                                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │  Web App    │  │ Desktop App │  │ Mobile App  │  │    CLI      │   │
│  │  (React)    │  │  (Tauri)    │  │  (Future)   │  │  (Future)   │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└───────────────────────────────┬─────────────────────────────────────────┘
                                │
                    ┌───────────┴───────────┐
                    │      API Gateway      │
                    │   (认证/限流/路由)     │
                    └───────────┬───────────┘
                                │
┌───────────────────────────────┴─────────────────────────────────────────┐
│                              服务层                                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │ 用户服务    │  │ Workflow    │  │  执行服务   │  │  社区服务   │   │
│  │ User        │  │  Service    │  │  Executor   │  │  Community  │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │ AI服务     │  │  插件服务   │  │  通知服务   │  │  计费服务   │   │
│  │ AI Gateway │  │  Plugin     │  │  Notify     │  │  Billing    │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└───────────────────────────────┬─────────────────────────────────────────┘
                                │
┌───────────────────────────────┴─────────────────────────────────────────┐
│                              数据层                                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │ PostgreSQL  │  │   Redis     │  │ ClickHouse  │  │     S3      │   │
│  │ (主数据库)  │  │  (缓存/队列)│  │ (分析日志)  │  │  (文件存储) │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.2 本地/云端双模式架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         部署模式选择                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   模式A: 纯本地                     模式B: 云端                          │
│   ┌─────────────────────┐          ┌─────────────────────┐             │
│   │   Desktop App       │          │    Web App          │             │
│   │   ┌─────────────┐   │          │    ┌─────────────┐  │             │
│   │   │ 本地执行引擎 │   │          │    │ 云端服务    │  │             │
│   │   │ SQLite      │   │          │    │ PostgreSQL  │  │             │
│   │   │ Ollama集成  │   │          │    │ 多模型API   │  │             │
│   │   └─────────────┘   │          │    └─────────────┘  │             │
│   │   数据完全本地      │          │    多设备同步       │             │
│   └─────────────────────┘          └─────────────────────┘             │
│                                                                         │
│   模式C: 混合模式（推荐）                                                │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                                                                 │  │
│   │   本地执行 ←───── 智能切换 ─────→ 云端执行                      │  │
│   │      ↓                              ↓                           │  │
│   │   敏感数据处理                    高性能计算                    │  │
│   │   离线可用                        模型丰富                      │  │
│   │                                                                 │  │
│   │              ↑         同步层         ↓                         │  │
│   │   ┌─────────────────────────────────────────────┐              │  │
│   │   │  • 配置同步（加密）                          │              │  │
│   │   │  • 模板同步                                  │              │  │
│   │   │  • 执行记录同步（可选）                      │              │  │
│   │   └─────────────────────────────────────────────┘              │  │
│   │                                                                 │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.3 插件系统架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           插件系统                                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   插件类型:                                                              │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │
│   │  节点插件   │  │  主题插件   │  │  集成插件   │  │  功能插件   │  │
│   │ 自定义节点  │  │  UI主题     │  │  第三方集成 │  │  扩展功能   │  │
│   └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘  │
│                                                                         │
│   插件生命周期:                                                          │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │  发现 → 安装 → 加载 → 激活 → 运行 → 停用 → 卸载                  │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│   安全机制:                                                              │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │  • 沙箱隔离执行                                                  │  │
│   │  • 权限声明和审核                                                │  │
│   │  • 资源使用限制                                                  │  │
│   │  • 代码签名验证                                                  │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 6. 功能模块详细说明

### 6.1 Workflow编辑器模块

#### 6.1.1 界面布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Logo   [我的工作流 ▼]  [新建]  [导入]     [运行 ▶]  [发布]    👤 用户  │
├─────────────────────────────────────────────────────────────────────────┤
│        │                                                    │           │
│  节    │                                                    │  配  │属  │
│  点    │                                                    │  置  │性  │
│  面    │                    画布区域                         │  面  │面  │
│  板    │                                                    │  板  │板  │
│        │    ┌─────┐       ┌─────┐       ┌─────┐           │      │    │
│  搜索  │    │Start│ ──→  │ LLM │ ──→  │ End │           │ 节点 │ 变  │
│  ────  │    └─────┘       └─────┘       └─────┘           │ 配置 │ 量  │
│  AI    │                                                    │      │    │
│  集成  │                                                    │ 输入 │ 日  │
│  数据  │                                                    │ 输出 │ 志  │
│  逻辑  │                                                    │      │    │
│  ...   │                                      [小地图]      │ 测试 │    │
│        │                                                    │      │    │
├─────────────────────────────────────────────────────────────────────────┤
│  状态: 已保存  │  节点: 3  │  最后运行: 2分钟前  │  版本: v1.2         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 6.1.2 节点配置面板

**通用配置项**:
- 节点名称（可自定义）
- 节点描述（备注）
- 启用/禁用开关
- 错误处理策略
- 超时设置

**输入输出映射**:
- 可视化数据映射
- 表达式编辑器
- 变量选择器
- 数据预览

### 6.2 Agent商店模块

#### 6.2.1 商店首页

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Agent 商店                                        │
├─────────────────────────────────────────────────────────────────────────┤
│  [搜索Agent...]                              [筛选 ▼] [排序 ▼]          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  📌 精选推荐                                                 [查看更多 →]│
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐      │
│  │ 📰          │ │ 📊          │ │ 🎨          │ │ 💼          │      │
│  │ 每日新闻    │ │ 数据分析    │ │ 设计助手    │ │ 商业计划    │      │
│  │ 摘要助手    │ │ 专家        │ │             │ │ 生成器      │      │
│  │ ⭐4.9 免费  │ │ ⭐4.8 ¥9.9 │ │ ⭐4.7 免费  │ │ ⭐4.6 ¥19.9│      │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘      │
│                                                                         │
│  🔥 热门分类                                                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │ 内容创作 │ │ 数据处理 │ │ 客服助手 │ │ 开发工具 │ │ 办公效率 │    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘    │
│                                                                         │
│  🆕 最新上架                                                            │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  [Agent列表...]                                                  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 6.2.2 Agent详情页

**展示内容**:
- Agent名称、图标、简介
- 作者信息、评分评价
- 使用次数、收藏数
- 功能说明、使用场景
- 截图/演示视频
- 定价信息
- 版本历史
- 用户评论

**操作按钮**:
- 一键使用（直接运行）
- 添加到我的工作流
- Fork并修改
- 查看源码（如果开源）
- 收藏、分享

### 6.3 自定义节点SDK

#### 6.3.1 SDK设计原则

- **简单直观**: 5分钟上手，写出第一个自定义节点
- **类型安全**: 完整的TypeScript支持
- **功能强大**: 支持复杂的异步操作、状态管理
- **安全隔离**: 沙箱执行，资源限制

#### 6.3.2 节点定义示例

```typescript
// @agentflow/sdk 节点定义API

import {
  defineNode,
  Input,
  Output,
  NodeContext,
  ValidationError
} from '@agentflow/sdk';

// 自定义节点: 翻译器
export default defineNode({
  // 基础信息
  id: 'custom-translator',
  name: '多语言翻译器',
  description: '使用AI将文本翻译成目标语言',
  icon: '🌐',
  category: 'AI处理',
  version: '1.0.0',
  author: 'your-username',
  
  // 输入定义
  inputs: {
    text: Input.text({
      label: '待翻译文本',
      required: true,
      placeholder: '输入需要翻译的文本...',
    }),
    
    sourceLang: Input.select({
      label: '源语言',
      options: [
        { value: 'auto', label: '自动检测' },
        { value: 'zh', label: '中文' },
        { value: 'en', label: '英文' },
        { value: 'ja', label: '日文' },
      ],
      default: 'auto',
    }),
    
    targetLang: Input.select({
      label: '目标语言',
      required: true,
      options: [
        { value: 'zh', label: '中文' },
        { value: 'en', label: '英文' },
        { value: 'ja', label: '日文' },
        { value: 'ko', label: '韩文' },
      ],
    }),
    
    style: Input.select({
      label: '翻译风格',
      options: [
        { value: 'formal', label: '正式' },
        { value: 'casual', label: '口语化' },
        { value: 'technical', label: '技术文档' },
      ],
      default: 'formal',
    }),
  },
  
  // 输出定义
  outputs: {
    translatedText: Output.text({
      label: '翻译结果',
    }),
    detectedLang: Output.text({
      label: '检测到的源语言',
    }),
    confidence: Output.number({
      label: '翻译置信度',
    }),
  },
  
  // 输入验证
  validate({ inputs }) {
    if (!inputs.text?.trim()) {
      throw new ValidationError('text', '翻译文本不能为空');
    }
    if (inputs.text.length > 10000) {
      throw new ValidationError('text', '文本长度不能超过10000字符');
    }
  },
  
  // 执行逻辑
  async execute({ inputs, context }: { inputs: any; context: NodeContext }) {
    const { text, sourceLang, targetLang, style } = inputs;
    
    // 使用context提供的LLM服务
    const result = await context.llm.chat({
      model: context.config.defaultModel || 'gpt-4',
      messages: [
        {
          role: 'system',
          content: `You are a professional translator. Translate the following text to ${targetLang} in a ${style} style. If source language is 'auto', detect it first.`
        },
        {
          role: 'user',
          content: text
        }
      ],
      responseFormat: {
        type: 'json',
        schema: {
          translatedText: 'string',
          detectedLang: 'string',
          confidence: 'number'
        }
      }
    });
    
    // 记录日志
    context.log.info('Translation completed', {
      sourceLang: result.detectedLang,
      targetLang,
      charCount: text.length
    });
    
    return {
      translatedText: result.translatedText,
      detectedLang: result.detectedLang,
      confidence: result.confidence,
    };
  },
  
  // 测试用例（可选）
  tests: [
    {
      name: '中文到英文',
      inputs: {
        text: '你好世界',
        sourceLang: 'auto',
        targetLang: 'en',
        style: 'casual',
      },
      expected: {
        detectedLang: 'zh',
      },
    },
  ],
});
```

#### 6.3.3 SDK能力清单

| 能力 | API | 说明 |
|------|-----|------|
| LLM调用 | `context.llm.chat()` | 调用大语言模型 |
| HTTP请求 | `context.http.request()` | 发送HTTP请求 |
| 文件操作 | `context.fs.read/write()` | 文件读写（沙箱内） |
| 缓存 | `context.cache.get/set()` | 临时数据缓存 |
| 日志 | `context.log.info/warn/error()` | 执行日志 |
| 密钥 | `context.secrets.get()` | 获取用户配置的密钥 |
| 事件 | `context.events.emit()` | 发送自定义事件 |
| 进度 | `context.progress.update()` | 更新执行进度 |

---

## 7. 技术规格

### 7.1 前端技术栈

```yaml
框架:
  - React 18 with TypeScript
  - Vite (构建工具)
  
状态管理:
  - Zustand (全局状态)
  - React Query (服务端状态)
  
UI组件:
  - Tailwind CSS (样式)
  - Radix UI (无障碍基础组件)
  - 自研设计系统
  
画布引擎:
  - React Flow (初期)
  - 自研引擎 (长期)
  
桌面端:
  - Tauri (跨平台桌面应用)
  
代码编辑:
  - Monaco Editor (代码节点)
  
工程化:
  - ESLint + Prettier
  - Vitest (单元测试)
  - Playwright (E2E测试)
  - Storybook (组件文档)
```

### 7.2 后端技术栈

```yaml
主框架:
  - Node.js 20 LTS
  - Fastify (HTTP框架) 或 Hono (轻量)
  - TypeScript
  
数据库:
  - PostgreSQL 16 (主数据库)
  - Redis 7 (缓存/队列)
  - ClickHouse (分析/日志)
  - SQLite (本地模式)
  
任务队列:
  - BullMQ (任务调度)
  - Temporal (复杂工作流，可选)
  
AI集成:
  - Vercel AI SDK
  - LangChain.js (复杂场景)
  - Ollama (本地模型)
  
认证:
  - Auth.js (NextAuth)
  - JWT + Refresh Token
  
API:
  - REST API (主要)
  - WebSocket (实时)
  - tRPC (类型安全RPC，可选)
  
测试:
  - Vitest
  - Supertest
```

### 7.3 基础设施

```yaml
云服务:
  - Vercel (前端部署)
  - Railway / Fly.io (后端服务)
  - Cloudflare R2 (对象存储)
  - Cloudflare (CDN/DNS)
  - Neon / Supabase (托管PostgreSQL)
  - Upstash (托管Redis)
  
监控:
  - Sentry (错误追踪)
  - PostHog (产品分析)
  - Grafana Cloud (指标监控)
  
CI/CD:
  - GitHub Actions
  - Docker (容器化)
  
私有化部署:
  - Docker Compose (单机)
  - Kubernetes Helm Chart (集群)
```

### 7.4 性能指标要求

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 首屏加载 | < 2s | 首次有意义绘制 |
| 编辑器响应 | < 100ms | 拖拽、连线操作 |
| API响应 | < 200ms | P95延迟 |
| 工作流启动 | < 500ms | 从触发到开始执行 |
| 节点执行 | < 30s | 单节点超时 |
| 并发执行 | 1000+ | 同时运行的工作流数 |
| 可用性 | 99.9% | 月度SLA |

---

## 8. 数据模型设计

### 8.1 核心实体关系图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           数据模型 ER 图                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│    ┌──────────┐       ┌──────────────┐       ┌──────────────┐         │
│    │   User   │──1:N──│   Workflow   │──1:N──│   Execution  │         │
│    └──────────┘       └──────────────┘       └──────────────┘         │
│         │                    │                      │                   │
│        1:N                  1:N                    1:N                  │
│         │                    │                      │                   │
│    ┌──────────┐       ┌──────────────┐       ┌──────────────┐         │
│    │  ApiKey  │       │     Node     │       │   NodeLog    │         │
│    └──────────┘       └──────────────┘       └──────────────┘         │
│         │                    │                                          │
│        1:N                  N:1                                         │
│         │                    │                                          │
│    ┌──────────┐       ┌──────────────┐       ┌──────────────┐         │
│    │  Team    │──N:M──│   Agent      │──1:N──│   Review     │         │
│    └──────────┘       └──────────────┘       └──────────────┘         │
│                              │                                          │
│                             1:N                                         │
│                              │                                          │
│                       ┌──────────────┐                                 │
│                       │   Version    │                                 │
│                       └──────────────┘                                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 8.2 主要数据表设计

#### 8.2.1 用户表 (users)

```sql
CREATE TABLE users (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email           VARCHAR(255) UNIQUE NOT NULL,
    username        VARCHAR(50) UNIQUE NOT NULL,
    display_name    VARCHAR(100),
    avatar_url      VARCHAR(500),
    bio             TEXT,
    
    -- 认证相关
    password_hash   VARCHAR(255),
    email_verified  BOOLEAN DEFAULT FALSE,
    
    -- 配置
    settings        JSONB DEFAULT '{}',
    
    -- 统计
    workflow_count  INTEGER DEFAULT 0,
    agent_count     INTEGER DEFAULT 0,
    follower_count  INTEGER DEFAULT 0,
    following_count INTEGER DEFAULT 0,
    
    -- 订阅
    plan            VARCHAR(20) DEFAULT 'free',
    plan_expires_at TIMESTAMPTZ,
    
    -- 时间戳
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    updated_at      TIMESTAMPTZ DEFAULT NOW(),
    last_login_at   TIMESTAMPTZ
);

-- 索引
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_username ON users(username);
```

#### 8.2.2 工作流表 (workflows)

```sql
CREATE TABLE workflows (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID NOT NULL REFERENCES users(id),
    
    -- 基础信息
    name            VARCHAR(200) NOT NULL,
    description     TEXT,
    icon            VARCHAR(50),
    
    -- 工作流定义
    definition      JSONB NOT NULL,  -- 节点和连线定义
    variables       JSONB DEFAULT '{}',  -- 全局变量
    
    -- 状态
    status          VARCHAR(20) DEFAULT 'draft',  -- draft, published, archived
    is_public       BOOLEAN DEFAULT FALSE,
    
    -- 执行配置
    trigger_type    VARCHAR(50),  -- manual, schedule, webhook, event
    trigger_config  JSONB DEFAULT '{}',
    
    -- 统计
    run_count       INTEGER DEFAULT 0,
    star_count      INTEGER DEFAULT 0,
    fork_count      INTEGER DEFAULT 0,
    
    -- 版本
    version         INTEGER DEFAULT 1,
    
    -- 隐私设置
    privacy_level   VARCHAR(20) DEFAULT 'private',
    
    -- 时间戳
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    updated_at      TIMESTAMPTZ DEFAULT NOW(),
    published_at    TIMESTAMPTZ,
    
    -- 文件夹
    folder_id       UUID REFERENCES folders(id)
);

-- 索引
CREATE INDEX idx_workflows_user ON workflows(user_id);
CREATE INDEX idx_workflows_status ON workflows(status);
CREATE INDEX idx_workflows_public ON workflows(is_public) WHERE is_public = TRUE;
```

#### 8.2.3 执行记录表 (executions)

```sql
CREATE TABLE executions (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    workflow_id     UUID NOT NULL REFERENCES workflows(id),
    user_id         UUID NOT NULL REFERENCES users(id),
    
    -- 执行状态
    status          VARCHAR(20) NOT NULL,  -- pending, running, completed, failed, cancelled
    
    -- 触发信息
    trigger_type    VARCHAR(50) NOT NULL,
    trigger_data    JSONB DEFAULT '{}',
    
    -- 执行数据
    inputs          JSONB DEFAULT '{}',
    outputs         JSONB DEFAULT '{}',
    context         JSONB DEFAULT '{}',
    
    -- 性能指标
    started_at      TIMESTAMPTZ,
    completed_at    TIMESTAMPTZ,
    duration_ms     INTEGER,
    
    -- 错误信息
    error_message   TEXT,
    error_stack     TEXT,
    error_node_id   VARCHAR(100),
    
    -- 资源消耗
    token_usage     JSONB DEFAULT '{}',
    api_calls       INTEGER DEFAULT 0,
    
    -- 时间戳
    created_at      TIMESTAMPTZ DEFAULT NOW()
);

-- 索引
CREATE INDEX idx_executions_workflow ON executions(workflow_id);
CREATE INDEX idx_executions_user ON executions(user_id);
CREATE INDEX idx_executions_status ON executions(status);
CREATE INDEX idx_executions_created ON executions(created_at);

-- 分区（按月）
-- 建议对大量执行记录做分区处理
```

#### 8.2.4 Agent表 (agents)

```sql
CREATE TABLE agents (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID NOT NULL REFERENCES users(id),
    workflow_id     UUID NOT NULL REFERENCES workflows(id),
    
    -- 基础信息
    name            VARCHAR(200) NOT NULL,
    slug            VARCHAR(100) UNIQUE NOT NULL,
    description     TEXT,
    long_description TEXT,
    icon            VARCHAR(50),
    cover_image     VARCHAR(500),
    
    -- 分类
    category        VARCHAR(50),
    tags            VARCHAR(50)[] DEFAULT '{}',
    
    -- 状态
    status          VARCHAR(20) DEFAULT 'draft',  -- draft, pending_review, published, rejected, archived
    
    -- 定价
    pricing_type    VARCHAR(20) DEFAULT 'free',  -- free, paid, subscription
    price           DECIMAL(10, 2),
    currency        VARCHAR(10) DEFAULT 'CNY',
    
    -- 统计
    use_count       INTEGER DEFAULT 0,
    star_count      INTEGER DEFAULT 0,
    review_count    INTEGER DEFAULT 0,
    avg_rating      DECIMAL(3, 2) DEFAULT 0,
    revenue         DECIMAL(12, 2) DEFAULT 0,
    
    -- 元数据
    screenshots     VARCHAR(500)[] DEFAULT '{}',
    demo_video      VARCHAR(500),
    
    -- 时间戳
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    updated_at      TIMESTAMPTZ DEFAULT NOW(),
    published_at    TIMESTAMPTZ
);

-- 索引
CREATE INDEX idx_agents_user ON agents(user_id);
CREATE INDEX idx_agents_category ON agents(category);
CREATE INDEX idx_agents_status ON agents(status);
CREATE INDEX idx_agents_slug ON agents(slug);
CREATE INDEX idx_agents_rating ON agents(avg_rating DESC);
```

#### 8.2.5 工作流定义JSON结构

```typescript
interface WorkflowDefinition {
  // 元数据
  version: string;  // "1.0.0"
  
  // 节点列表
  nodes: {
    id: string;
    type: string;  // "llm", "http", "condition", etc.
    position: { x: number; y: number };
    data: {
      label: string;
      config: Record<string, any>;  // 节点特定配置
    };
  }[];
  
  // 连线列表
  edges: {
    id: string;
    source: string;      // 源节点ID
    sourceHandle: string; // 源端口
    target: string;      // 目标节点ID
    targetHandle: string; // 目标端口
    label?: string;
  }[];
  
  // 全局设置
  settings: {
    timeout: number;
    retryPolicy: {
      maxRetries: number;
      backoffMs: number;
    };
    errorHandling: 'stop' | 'continue' | 'fallback';
  };
}
```

---

## 9. API设计规范

### 9.1 API设计原则

- RESTful风格
- 版本控制 (v1, v2...)
- 统一响应格式
- 完善的错误处理
- 请求限流
- 完整的OpenAPI文档

### 9.2 认证与授权

```yaml
认证方式:
  - Bearer Token (JWT)
  - API Key (服务间调用)

请求头:
  Authorization: Bearer <token>
  X-API-Key: <api_key>

Token刷新:
  - Access Token: 15分钟
  - Refresh Token: 7天

权限模型:
  - RBAC (基于角色的访问控制)
  - 资源级别权限
```

### 9.3 核心API端点

#### 9.3.1 工作流相关

```yaml
# 工作流CRUD
GET    /api/v1/workflows                 # 获取工作流列表
POST   /api/v1/workflows                 # 创建工作流
GET    /api/v1/workflows/:id             # 获取工作流详情
PUT    /api/v1/workflows/:id             # 更新工作流
DELETE /api/v1/workflows/:id             # 删除工作流

# 工作流操作
POST   /api/v1/workflows/:id/execute     # 执行工作流
POST   /api/v1/workflows/:id/duplicate   # 复制工作流
POST   /api/v1/workflows/:id/publish     # 发布为Agent
GET    /api/v1/workflows/:id/versions    # 获取版本历史
POST   /api/v1/workflows/:id/rollback    # 回滚版本

# 执行相关
GET    /api/v1/executions                # 获取执行记录列表
GET    /api/v1/executions/:id            # 获取执行详情
GET    /api/v1/executions/:id/logs       # 获取执行日志
POST   /api/v1/executions/:id/cancel     # 取消执行
POST   /api/v1/executions/:id/retry      # 重试执行
```

#### 9.3.2 Agent商店相关

```yaml
# Agent浏览
GET    /api/v1/agents                    # 获取Agent列表
GET    /api/v1/agents/featured           # 获取精选Agent
GET    /api/v1/agents/categories         # 获取分类列表
GET    /api/v1/agents/:slug              # 获取Agent详情

# Agent操作
POST   /api/v1/agents/:id/use            # 使用Agent
POST   /api/v1/agents/:id/fork           # Fork Agent
POST   /api/v1/agents/:id/star           # 收藏Agent
POST   /api/v1/agents/:id/review         # 评价Agent

# 创作者
POST   /api/v1/agents                    # 发布Agent
PUT    /api/v1/agents/:id                # 更新Agent
GET    /api/v1/agents/:id/analytics      # 获取分析数据
```

### 9.4 响应格式

```typescript
// 成功响应
interface SuccessResponse<T> {
  success: true;
  data: T;
  meta?: {
    total?: number;
    page?: number;
    pageSize?: number;
  };
}

// 错误响应
interface ErrorResponse {
  success: false;
  error: {
    code: string;      // 错误码，如 "WORKFLOW_NOT_FOUND"
    message: string;   // 用户友好的错误信息
    details?: any;     // 详细错误信息（开发模式）
  };
}

// 示例
// 成功
{
  "success": true,
  "data": {
    "id": "wf_xxx",
    "name": "我的工作流",
    ...
  }
}

// 错误
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "工作流名称不能为空",
    "details": {
      "field": "name",
      "constraint": "required"
    }
  }
}
```

### 9.5 WebSocket API

```yaml
# 连接
ws://api.agentflow.app/ws?token=<jwt>

# 事件类型
events:
  # 客户端 -> 服务端
  - subscribe:execution     # 订阅执行状态
  - unsubscribe:execution   # 取消订阅
  
  # 服务端 -> 客户端
  - execution:started       # 执行开始
  - execution:node_started  # 节点开始
  - execution:node_completed # 节点完成
  - execution:node_failed   # 节点失败
  - execution:completed     # 执行完成
  - execution:failed        # 执行失败
  - execution:log           # 实时日志

# 消息格式
{
  "type": "execution:node_completed",
  "payload": {
    "executionId": "exec_xxx",
    "nodeId": "node_1",
    "outputs": { ... },
    "duration": 1234
  },
  "timestamp": "2026-01-26T10:00:00Z"
}
```

---

## 10. 安全与隐私

### 10.1 安全架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           安全架构                                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        应用安全                                  │   │
│  │  • 输入验证 & 输出编码                                          │   │
│  │  • CSRF保护                                                     │   │
│  │  • XSS防护                                                      │   │
│  │  • SQL注入防护                                                  │   │
│  │  • 速率限制                                                     │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        数据安全                                  │   │
│  │  • 传输加密 (TLS 1.3)                                          │   │
│  │  • 存储加密 (AES-256)                                          │   │
│  │  • 密钥管理 (HashiCorp Vault / AWS KMS)                        │   │
│  │  • 敏感数据脱敏                                                 │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        执行安全                                  │   │
│  │  • 代码沙箱隔离                                                 │   │
│  │  • 资源使用限制                                                 │   │
│  │  • 网络访问控制                                                 │   │
│  │  • 执行超时机制                                                 │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        访问控制                                  │   │
│  │  • 身份认证 (MFA支持)                                          │   │
│  │  • 细粒度权限 (RBAC)                                           │   │
│  │  • API密钥管理                                                  │   │
│  │  • 会话管理                                                     │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 10.2 隐私保护

#### 10.2.1 数据分级

| 级别 | 数据类型 | 存储方式 | 示例 |
|------|----------|----------|------|
| L1 - 公开 | 公开Agent信息 | 云端 | Agent名称、描述 |
| L2 - 内部 | 用户基础信息 | 云端加密 | 邮箱、用户名 |
| L3 - 敏感 | API密钥、认证信息 | 加密存储 | OpenAI Key |
| L4 - 机密 | 用户私有数据 | 本地或E2E加密 | 私有工作流数据 |

#### 10.2.2 隐私设置选项

```typescript
interface PrivacySettings {
  // 数据存储位置
  storagePreference: 'cloud' | 'local' | 'hybrid';
  
  // 同步设置
  sync: {
    enabled: boolean;
    encryptBeforeSync: boolean;  // 端到端加密
    syncExecutionLogs: boolean;
  };
  
  // 分析数据
  analytics: {
    allowAnonymousUsage: boolean;
    allowCrashReports: boolean;
  };
  
  // 执行设置
  execution: {
    preferLocalLLM: boolean;      // 优先本地模型
    allowCloudExecution: boolean;
  };
}
```

### 10.3 合规要求

- **GDPR**: 数据可移植性、删除权、访问权
- **个人信息保护法**: 中国境内数据存储、用户同意
- **SOC 2**: 安全审计（企业版）

---

## 11. 商业模式

### 11.1 产品层级

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          产品层级设计                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                     Enterprise 企业版                            │  │
│   │  ¥2999+/月 (按需定价)                                           │  │
│   │  ─────────────────────────────────────────────────────────────  │  │
│   │  • 私有化部署                    • 无限工作流                    │  │
│   │  • SSO/LDAP集成                 • 专属客户成功经理              │  │
│   │  • 高级权限管理                  • SLA保障                      │  │
│   │  • 审计日志                      • 定制开发                     │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                        Pro 专业版                                │  │
│   │  ¥99/月 或 ¥999/年                                              │  │
│   │  ─────────────────────────────────────────────────────────────  │  │
│   │  • 无限工作流                    • 高级节点                      │  │
│   │  • 团队协作 (5人)                • 优先执行队列                  │  │
│   │  • 高级分析                      • 更多API调用量                 │  │
│   │  • 自定义节点发布                • 专属支持                     │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                        Free 免费版                               │  │
│   │  ¥0 永久免费                                                    │  │
│   │  ─────────────────────────────────────────────────────────────  │  │
│   │  • 10个工作流                    • 基础节点                      │  │
│   │  • 每月1000次执行                • 社区支持                      │  │
│   │  • 基础分析                      • 本地模式完全免费              │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 11.2 收入来源

```
收入构成预估
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  订阅收入 (60%)                                                         │
│  ████████████████████████████████████░░░░░░░░░░░░░░░░░░░░░░░░           │
│  • Pro订阅                                                              │
│  • 企业订阅                                                              │
│                                                                         │
│  交易分成 (25%)                                                         │
│  ███████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░           │
│  • Agent商店交易抽成 (30%)                                              │
│  • 插件商店交易抽成                                                     │
│                                                                         │
│  增值服务 (10%)                                                         │
│  ██████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░           │
│  • API调用超额                                                          │
│  • 高级支持                                                             │
│  • 定制开发                                                             │
│                                                                         │
│  其他 (5%)                                                              │
│  ███░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░           │
│  • 培训/咨询                                                            │
│  • 品牌合作                                                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 11.3 创作者经济

| 类型 | 创作者分成 | 平台分成 | 说明 |
|------|-----------|----------|------|
| 付费Agent | 70% | 30% | 单次购买或订阅 |
| 付费插件 | 70% | 30% | 节点插件销售 |
| 小费/打赏 | 90% | 10% | 用户自愿打赏 |
| 推广收益 | 50% | 50% | 推广返佣 |

**创作者激励计划**:
- 新创作者首3个月0%平台分成
- 月收入超过¥10000，分成提升至75%
- 优质创作者专属流量扶持

---

## 12. 开发路线图

### 12.1 总体时间线

```
2026 Q1          2026 Q2          2026 Q3          2026 Q4
  │                │                │                │
  ▼                ▼                ▼                ▼
┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│  Phase 1    │  │  Phase 2    │  │  Phase 3    │  │  Phase 4    │
│  核心MVP    │→│  自定义能力  │→│  社区生态   │→│  规模化     │
│             │  │             │  │             │  │             │
│ • 编辑器    │  │ • 节点SDK   │  │ • Agent商店 │  │ • 企业版    │
│ • 基础节点  │  │ • 插件系统  │  │ • 分享机制  │  │ • 团队协作  │
│ • 执行引擎  │  │ • 本地模型  │  │ • 创作者   │  │ • 私有部署  │
│ • 用户系统  │  │ • 调试工具  │  │   经济     │  │ • 高级分析  │
└─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘
     ↓                ↓                ↓                ↓
  内测发布         公测发布         正式发布         商业化
```

### 12.2 Phase 1: 核心MVP (2026 Q1)

**目标**: 可用的工作流编辑器和执行引擎

**里程碑**:

| 周次 | 里程碑 | 交付物 |
|------|--------|--------|
| W1-2 | 项目启动 | 技术选型、开发环境、CI/CD |
| W3-4 | 编辑器基础 | 画布、节点拖拽、连线 |
| W5-6 | 基础节点 | LLM、HTTP、条件、循环节点 |
| W7-8 | 执行引擎 | 任务调度、状态管理 |
| W9-10 | 用户系统 | 注册登录、工作流管理 |
| W11-12 | 打磨优化 | Bug修复、性能优化、内测发布 |

**关键指标**:
- 编辑器能正常创建和保存工作流
- 至少10种基础节点可用
- 工作流可成功执行并查看结果
- 内测用户满意度 > 70%

### 12.3 Phase 2: 自定义能力 (2026 Q2)

**目标**: 开放自定义能力，吸引开发者

**里程碑**:

| 周次 | 里程碑 | 交付物 |
|------|--------|--------|
| W1-3 | 节点SDK | SDK设计、开发者文档 |
| W4-6 | 插件系统 | 加载器、沙箱、基础市场 |
| W7-8 | 本地模式 | 桌面应用、本地LLM集成 |
| W9-10 | 调试工具 | 断点、日志、性能分析 |
| W11-12 | 公测发布 | 公测、反馈收集、迭代 |

**关键指标**:
- SDK文档完整，开发者可独立创建节点
- 至少20个第三方插件上线
- 本地模式可完全离线运行
- 公测用户 > 1000

### 12.4 Phase 3: 社区生态 (2026 Q3)

**目标**: 建立社区飞轮，实现有机增长

**里程碑**:

| 周次 | 里程碑 | 交付物 |
|------|--------|--------|
| W1-3 | Agent商店 | 发布流程、审核系统 |
| W4-6 | 分享机制 | 链接分享、嵌入预览、Fork |
| W7-9 | 创作者经济 | 支付系统、分成结算 |
| W10-12 | 正式发布 | 全量上线、运营活动 |

**关键指标**:
- Agent商店上架 > 100个Agent
- 日活跃用户 > 5000
- 创作者 > 200人
- NPS > 50

### 12.5 Phase 4: 规模化 (2026 Q4)

**目标**: 商业化变现，企业市场拓展

**里程碑**:

| 周次 | 里程碑 | 交付物 |
|------|--------|--------|
| W1-4 | 企业版 | 私有部署、SSO、审计 |
| W5-8 | 团队协作 | 多人协作、权限管理 |
| W9-12 | 高级功能 | 分析、监控、优化 |

**关键指标**:
- 付费用户 > 500
- MRR > ¥100,000
- 企业客户 > 10家
- Agent交易额 > ¥500,000

---

## 13. 成功指标

### 13.1 北极星指标

> **周活跃执行的工作流数 (Weekly Active Workflows)**

选择理由:
- 反映产品核心价值：用户持续使用工作流解决问题
- 兼顾用户粘性和产品实用性
- 可拆解为多个过程指标

### 13.2 关键指标体系

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          指标体系                                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  用户获取 (Acquisition)                                                 │
│  ├── 新注册用户数                                                       │
│  ├── 注册转化率                                                         │
│  └── 获客成本 (CAC)                                                     │
│                                                                         │
│  用户激活 (Activation)                                                  │
│  ├── 激活率 (创建首个工作流)                                            │
│  ├── 首次执行时间                                                       │
│  └── 新用户引导完成率                                                   │
│                                                                         │
│  用户留存 (Retention)                                                   │
│  ├── D1/D7/D30 留存率                                                   │
│  ├── 月活跃用户 (MAU)                                                   │
│  └── 用户生命周期价值 (LTV)                                             │
│                                                                         │
│  营收 (Revenue)                                                         │
│  ├── 月度经常性收入 (MRR)                                               │
│  ├── 付费转化率                                                         │
│  ├── ARPU (每用户平均收入)                                              │
│  └── Agent商店GMV                                                       │
│                                                                         │
│  推荐 (Referral)                                                        │
│  ├── NPS (净推荐值)                                                     │
│  ├── 邀请转化率                                                         │
│  └── 分享带来的新用户数                                                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 13.3 各阶段目标

| 阶段 | 时间 | MAU | WAW | 付费用户 | MRR |
|------|------|-----|-----|----------|-----|
| 内测 | Q1末 | 500 | 200 | - | - |
| 公测 | Q2末 | 5,000 | 2,000 | 100 | ¥10,000 |
| 正式 | Q3末 | 20,000 | 10,000 | 500 | ¥50,000 |
| 增长 | Q4末 | 50,000 | 30,000 | 2,000 | ¥200,000 |

---

## 14. 风险评估

### 14.1 风险矩阵

```
影响大 ↑
       │  ┌─────────────────┐   ┌─────────────────┐
       │  │  R1: 技术复杂度  │   │  R2: 竞争加剧   │
       │  │  可能性: 中      │   │  可能性: 高     │
       │  │  影响: 高       │   │  影响: 高       │
       │  └─────────────────┘   └─────────────────┘
       │  ┌─────────────────┐   ┌─────────────────┐
       │  │  R3: 资金不足   │   │  R4: 用户增长慢 │
       │  │  可能性: 中      │   │  可能性: 中     │
       │  │  影响: 中       │   │  影响: 中       │
       │  └─────────────────┘   └─────────────────┘
影响小 └──────────────────────────────────────────→ 可能性高
```

### 14.2 风险应对策略

| 风险 | 描述 | 应对策略 |
|------|------|----------|
| **R1: 技术复杂度** | 双模式架构、插件沙箱等技术挑战 | MVP先做云端，逐步添加本地能力；插件先做白名单审核 |
| **R2: 竞争加剧** | 大厂入场、现有产品升级 | 聚焦差异化（本地+自定义）；建立社区护城河 |
| **R3: 资金不足** | 开发周期长，变现周期不确定 | 控制初期团队规模；尽早启动付费验证；寻求融资 |
| **R4: 用户增长慢** | 市场教育成本高、获客难 | 依靠社区和内容营销；与KOL合作；开源核心吸引开发者 |
| **R5: AI成本高** | LLM API费用影响利润 | 支持本地模型；优化prompt减少token；按用量收费 |
| **R6: 数据安全** | 用户数据泄露风险 | 加密存储；最小权限原则；定期安全审计 |

---

## 15. 附录

### 15.1 术语表

| 术语 | 英文 | 解释 |
|------|------|------|
| 工作流 | Workflow | 由多个节点和连线组成的自动化流程 |
| 节点 | Node | 工作流中的单个操作单元 |
| Agent | Agent | 发布到商店的可复用工作流 |
| 触发器 | Trigger | 启动工作流执行的条件或事件 |
| 执行 | Execution | 工作流的一次运行实例 |
| 插件 | Plugin | 扩展平台功能的第三方代码包 |
| 沙箱 | Sandbox | 隔离执行用户代码的安全环境 |
| E2E加密 | End-to-End Encryption | 端到端加密，数据仅用户可解密 |

### 15.2 参考资料

- [React Flow文档](https://reactflow.dev/)
- [Temporal工作流引擎](https://temporal.io/)
- [Tauri跨平台框架](https://tauri.app/)
- [Ollama本地LLM](https://ollama.ai/)
- [Vercel AI SDK](https://sdk.vercel.ai/)

### 15.3 设计资源

- Figma设计稿: [待创建]
- 组件库文档: [待创建]
- 品牌指南: [待创建]

### 15.4 变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-01-26 | 初始版本 | - |

---

## 联系方式

- **产品负责人**: [待定]
- **技术负责人**: [待定]
- **项目仓库**: [待创建]

---

> 本文档为产品需求的第一版，将随着项目进展持续更新迭代。
